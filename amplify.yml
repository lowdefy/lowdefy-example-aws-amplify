version: 1
  - appRoot: deploy
    frontend:
      phases:
        preBuild:
          commands:
            - corepack enable
            - pnpm i
            - pnpm build:lowdefy
        build:
          commands:
            - pnpm build
      artifacts:
        baseDirectory: .next
        files:
          - '**/*'
      cache:
        paths:
          - node_modules/**/*
